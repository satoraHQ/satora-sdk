---
"@satora/swap": patch
---

Rootstock testnet (chain `"31"`). The EVM tracker knows its block cadence,
public RPC and confirmation depth, so swaps a testnet daemon reports on
chain `"31"` are tracked like mainnet Rootstock ones.
